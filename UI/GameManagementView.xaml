<UserControl x:Class="GameLauncher.UI.GameManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameLauncher.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button x:Name="BackButton" Content="Back" Width="80" Height="30" Margin="5" Click="BackButton_Click"/>
            <TextBlock Text="Game Library" FontSize="24" Foreground="White" VerticalAlignment="Center" Margin="20,0,0,0"/>
        </StackPanel>

        <!-- Game List -->
        <ListView x:Name="GamesListView" Grid.Row="1" Margin="10" SelectionMode="Single" SelectionChanged="GamesListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Icon" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding IconSource}" Width="40" Height="40" Stretch="Uniform"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Path" Width="300" DisplayMemberBinding="{Binding ExecutablePath}"/>
                    <GridViewColumn Header="Play Time" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PlayTime, StringFormat='{}{0:hh\\:mm}'}" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Button Panel -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="AddNewGameButton" Content="Add New Game" Width="120" Height="30" Margin="5" Click="AddNewGameButton_Click"/>
            <Button x:Name="PlayButton" Content="Play" Width="80" Height="30" Margin="5" Click="PlayButton_Click"/>
            <Button x:Name="SelectButton" Content="Select" Width="80" Height="30" Margin="5" Click="SelectButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>